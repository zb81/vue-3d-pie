export interface Props {
  /** 是否开启抗锯齿 */
  antialias?: boolean
  /** 背景颜色，默认为 `#1f2937` */
  backgroundColor?: string
  /** 是否开启视角控制 */
  orbitControlsEnabled?: boolean
  /** 调色盘 */
  palette?: string[]
}
